# Changes

## Unreleased

- Add stable LSD radix sorts for `int`, `int64`, and unboxed `float` arrays.
- Add pass skipping and 11-bit digits.
- Add an in-place MSD/American-flag string sort.
- Add optional Domain-parallel numeric sorts for OCaml 5.
- Add correctness tests and standalone comparative benchmarks.
